@model SellingReport.Models.ViewModels.ProductSellingMonthlyReportViewModel

@{
    ViewBag.Title = "Edit";
}
@{
    Html.EnableClientValidation(false);
}
<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ProductSellingReport</legend>

        @Html.HiddenFor(model => model.ProductSellingMonthlyReport.ProductSellingMonthlyReportId)

        <div class="editor-label">
            @Html.LabelFor(model => model.ProductSellingMonthlyReport.CountryId)
        </div>
        <div class="editor-field">
            @Html.DropDownListFor(x => x.ProductSellingMonthlyReport.CountryId, Model.Countries)
        </div>
        <div class="editor-label">
            @Html.LabelFor(model => model.ProductSellingMonthlyReport.Date)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ProductSellingMonthlyReport.Date)
        </div>
        <div class="editor-label">
            @Html.LabelFor(model => model.ProductSellingMonthlyReport.SoldValue)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ProductSellingMonthlyReport.SoldValue)
            @Html.ValidationMessageFor(model => model.ProductSellingMonthlyReport.SoldValue)
        </div>
        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/jqueryui")
    @Scripts.Render("~/bundles/productsellingmonthlyreport")
}
